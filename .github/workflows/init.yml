name: CI
on:
  push:
  workflow_dispatch:
jobs:
  build:
    runs-on: ubuntu-latest
    container: ghcr.io/fwilhe2/k:bootstrap
    steps:
    - uses: actions/checkout@v2
    - name: test
      run: |
        kotlin jvm/main-kts/scripts/kotlin-shell.main.kts
